{
  "name": "avogado6",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "web:scrap": "bun web/scrap.js",
    "web:download": "bun web/download.js",
    "web": "bun run web:scrap && bun run web:download",
    "x:scrap": "bun x/scrap.js",
    "x:scrap-group": "bun x/scrapGroup.js",
    "x:download": "bun x/download.js",
    "x": "bun run x:scrap && bun run x:scrap-group && bun run x:download"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "dotenv": "^16.4.5",
    "puppeteer": "^22.6.1"
  }
}